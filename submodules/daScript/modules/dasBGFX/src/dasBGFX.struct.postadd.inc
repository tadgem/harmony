// this file is generated via daScript automatic C++ binder
// all user modifications will be lost after this file is re-generated

initRecAnnotation(ann_bgfx_dynamic_index_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_dynamic_vertex_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_frame_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_index_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_indirect_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_occlusion_query_handle_s,lib);
initRecAnnotation(ann_bgfx_program_handle_s,lib);
initRecAnnotation(ann_bgfx_shader_handle_s,lib);
initRecAnnotation(ann_bgfx_texture_handle_s,lib);
initRecAnnotation(ann_bgfx_uniform_handle_s,lib);
initRecAnnotation(ann_bgfx_vertex_buffer_handle_s,lib);
initRecAnnotation(ann_bgfx_vertex_layout_handle_s,lib);
initRecAnnotation(ann_bgfx_caps_gpu_s,lib);
initRecAnnotation(ann_bgfx_caps_limits_s,lib);
initRecAnnotation(ann_bgfx_caps_s,lib);
initRecAnnotation(ann_bgfx_internal_data_s,lib);
initRecAnnotation(ann_bgfx_platform_data_s,lib);
initRecAnnotation(ann_bgfx_resolution_s,lib);
initRecAnnotation(ann_bgfx_init_limits_s,lib);
initRecAnnotation(ann_bgfx_init_s,lib);
initRecAnnotation(ann_bgfx_memory_s,lib);
initRecAnnotation(ann_bgfx_transient_index_buffer_s,lib);
initRecAnnotation(ann_bgfx_transient_vertex_buffer_s,lib);
initRecAnnotation(ann_bgfx_instance_data_buffer_s,lib);
initRecAnnotation(ann_bgfx_texture_info_s,lib);
initRecAnnotation(ann_bgfx_uniform_info_s,lib);
initRecAnnotation(ann_bgfx_attachment_s,lib);
initRecAnnotation(ann_bgfx_transform_s,lib);
initRecAnnotation(ann_bgfx_view_stats_s,lib);
initRecAnnotation(ann_bgfx_encoder_stats_s,lib);
initRecAnnotation(ann_bgfx_stats_s,lib);
initRecAnnotation(ann_bgfx_vertex_layout_s,lib);
