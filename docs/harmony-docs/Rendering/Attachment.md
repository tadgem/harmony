An attachment is a texture with a specific texture format and resolution that can be attached to a Framebuffer. A typical example would be a framebuffer with a Color & Depth Attachment. The color attachment will be the "output" of the framebuffer, compromising each pixels color, where the depth attachment is used internally within the rendering pipeline to sort which pixels should and should not be drawn, avoiding overlapping geometry.  